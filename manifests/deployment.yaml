apiVersion: apps/v1
kind: Deployment
metadata:
  generation: 1
  labels:
    app: sample
  name: sample
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample
  template:
    metadata:
      labels:
        app: sample
    spec:
      containers:
      - image: docker.pkg.github.com/kaluzaaa/sample-net-core-docker-actions-package-registry/sample:dummy
        name: sample
